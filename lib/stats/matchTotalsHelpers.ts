export function getMatchAttackTotals(matchStats: any[], hiddenStats: string[] = []) {
  // devuelve los mismos KPIs que usa MatchAttackTotals
}

export function getMatchDefenseTotals(matchStats: any[], hiddenStats: string[] = []) {
  // devuelve los mismos KPIs que usa MatchDefenseTotals
}

export function getMatchGoalkeeperTotals(matchStats: any[], hiddenStats: string[] = []) {
  // devuelve los mismos KPIs que usa MatchGoalkeeperTotals
}